version: '2'
services:
 mongo:
  image: mongo
  volumes:
   - /data/mongodb/db:/data/db
  ports:
   - "27017:27017"


 api:
  build: .
  command: node ./bin/www
  volumes:
   - .:/fixit-app
  ports:
   - "80:80"
  links:
   - mongo
