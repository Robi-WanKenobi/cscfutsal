import { FilterStatsPipe } from './filter-stats.pipe';

describe('FilterStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
